{
  "name": "lead-enrichment-pipeline",
  "version": "0.1.0",
  "description": "Portfolio project: Airtable lead created -> enrich company data -> alert Discord -> write enrichment back to Airtable",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "setup-webhook": "node scripts/setupWebhook.js",
    "refresh-webhook": "node src/jobs/refreshWebhook.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
